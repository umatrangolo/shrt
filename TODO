# TODO

v REST routes:
v API for adding a shortened URL:
v API to get a shortened URL:
v API for deleting a shrt url
v API for listing all the shrt urls
v API for getting the most popualr shorts
v Some HATEOAS
v Store Shrts in H2
v Count each time a token/url is used
v On sending URL wout the schema assume that it is 'http://'
v Support for tags (then used for Search)
v Does not save the description!
v Make it possible to propose the token the Shrt has to be saved with
x Use H2 internal search engine to index shrts
  H2 full text search does not support wildcard search (e.g. q='goo' does not match 'google')
v Use Lucene to index Shrt(s)
@ Use Lucene Lookup module to provide auto-completion
+ AngularJS based UI
+ Better analytics
+ Performance ??
+ Get rid of the DB and use Lucene index for everything
v Shrt directive does not show tags
+ Better shrt directive with the analytics on the left
